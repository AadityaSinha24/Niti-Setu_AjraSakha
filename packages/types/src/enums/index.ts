export enum Role {
  FARMER = 'farmer',
  ADMIN = 'admin',
  AUDITOR = 'auditor'
}

export enum SocialCategory {
  GENERAL = 'General',
  SC = 'SC',
  ST = 'ST',
  OBC = 'OBC'
}

export enum Language {
  EN = 'en',
  HI = 'hi',
  MR = 'mr'
}
